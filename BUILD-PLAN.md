# نقشه‌ی ساخت سایت جدید اپن‌تاپ (بازمهندسی kettal.com)

تاریخ: ۲۰۲۶-۰۸-۱۴ · تهیه: کلود · وضعیت: تحلیل فنی کامل شد، آماده‌ی شروع ساخت

---

## چرا ChatGPT نتونست

چون سعی کرده HTML رو کپی کنه. ولی این سایت یه **Nuxt 3 (Vue 3)** ـه که تمام انیمیشن‌هاش با **GSAP + ScrollTrigger** ران‌تایم ساخته می‌شن — توی HTML خروجی هیچ اثری ازشون نیست. کپی‌کردن HTML یعنی گرفتن جسد سایت بدون سیستم عصبیش.

راه درست: **سیستم رو بازسازی کنیم، نه فایل رو.** خبر خوب اینه که کل سیستم رو رمزگشایی کردم و از چیزی که به‌نظر می‌رسه خیلی ساده‌تره.

---

## استک واقعی سایت (کشف‌شده از فایل‌های دانلودی)

| لایه | تکنولوژی |
|---|---|
| فریم‌ورک | Nuxt 3 / Vue 3 (SSR) |
| انیمیشن | GSAP + ScrollTrigger (نسخه ۲۰۲۵) |
| CMS | DatoCMS (headless) |
| تصاویر | datocms-assets CDN + `<video>` با اسکراب |
| باندل JS | ۵۸۵KB (که بخش اعظمش خود GSAP ـه) |
| CSS | ۱۸۶KB در ۴۰+ فایل ماژولار |

---

## 🔑 دو کشف کلیدی (اینا کل بازی رو عوض می‌کنن)

### ۱. سیستم مقیاس سیال — راز تطابق دقیق دیزاین

```css
html { font-size: 0.0694444444vw; }          /* دسکتاپ */
@media (max-width: 960px) { html { font-size: 0.2666666667vw; } }  /* موبایل */
```

یعنی چی؟ `1440 × 0.0694444 = 100`... در واقع **در عرض ۱۴۴۰px، هر `1rem` دقیقاً برابر `1px` می‌شه**. و در موبایل، در عرض ۳۷۵px هم همینطور.

نتیجه: طراح توی فیگما با پیکسل کار می‌کنه، برنامه‌نویس همون عدد رو با `rem` می‌نویسه، و **کل سایت خودکار روی هر سایزی سیال اسکیل می‌شه** — بدون هیچ برک‌پوینتی. برای همینه که عددهای عجیبی مثل `334rem` و `414rem` توی CSS‌شون هست.

این تکنیک به‌تنهایی ۸۰٪ دردسر ریسپانسیو رو حذف می‌کنه. حتماً باید استفاده کنیم.

### ۲. یک منحنی، برای همه چیز

```css
--the-curve: cubic-bezier(.785, .135, .15, .86);   /* easeInOutCirc */
```

تمام انیمیشن‌های سایت از همین یک easing استفاده می‌کنن. اون حس «گران‌قیمت و یکدست» که داره، ۹۰٪ از همینه.

---

## سیستم انیمیشن — رمزگشایی‌شده

الگوی اصلی ساده‌ست: **IntersectionObserver کلاس `.is-revealed` رو اضافه می‌کنه، CSS بقیه‌ی کار رو می‌کنه.**

```css
/* حالت اولیه — کلیپ‌شده به پایین (نامرئی) */
.c-module-hero-simple .pin > .bg {
  clip-path: polygon(0 100%, 100% 100%, 100% 100%, 0 100%);
  transition: clip-path 1s var(--the-curve);
}
/* بعد از ورود به ویوپورت — باز شدن کامل */
.c-module-hero-simple .pin > .bg.is-revealed {
  clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
}
```

این «پرده‌ی بالارونده» امضای بصری کل سایته. یه واریانت افقی هم دارن که از چپ باز می‌شه: `polygon(0 0, 0 0, 0 100%, 0 100%)`.

### خانواده‌ی کامپوننت‌های ریویل (با تعداد استفاده در صفحه اول)

| کامپوننت | کاربرد | تعداد |
|---|---|---|
| `c-reveal-underline` | آندرلاین انیمیشنی لینک‌ها | ۱۱۶ |
| `c-media-asset` | ریویل کلیپ‌پث عکس/ویدیو | ۷۳ |
| `c-text-breaker` | شکستن متن برای انیمیشن | ۲۹ |
| `c-reveal-chars` | ظاهرشدن حرف‌به‌حرف | ۱۸ |
| `c-reveal-text-lines` | ظاهرشدن خط‌به‌خط | ۶ |
| `c-reveal-transform` | جابه‌جایی/اسکیل | ۳ |
| `c-reveal-blocks` | ظاهرشدن بلوکی | ۲ |

---

## ماژول‌های صفحه اول (۹ تا — کل صفحه همینه)

| # | ماژول | چیکار می‌کنه | سختی |
|---|---|---|---|
| ۱ | `hero-home` | ویدیوی تمام‌صفحه با اسکراب اسکرولی | 🔴 سخت |
| ۲ | `spotlight` | تیتر بزرگ با ریویل حرف‌به‌حرف | 🟢 آسان |
| ۳ | `products-showroom` | گرید پراکنده‌ی محصولات با پارالاکس | 🟡 متوسط |
| ۴ | `card-group-prominent` | کارت‌های بزرگ محصول | 🟢 آسان |
| ۵ | `materials-list` | گرید متریال/رنگ | 🟢 آسان |
| ۶ | `topic-text-r` | متن + عکس | 🟢 آسان |
| ۷ | `card-group-stairs` | کارت‌های پله‌ای/آفست | 🟡 متوسط |
| ۸ | `topic-parallax` | بخش پارالاکس | 🟡 متوسط |
| ۹ | `banner-single-image` | بنر تمام‌عرض | 🟢 آسان |

بعلاوه‌ی چیزهای مشترک: `c-layout-header` (نav چسبان)، `c-layout-footer`، `c-layout-loader` (لودر ورودی)، `c-layout-sidebar`، `c-modal-popup`، `c-carousel-simple`.

---

## تقسیم کار: چی رو من می‌سازم، چی رو نه

### ✅ من مستقیم می‌سازم (۹۵٪ کار)

همه‌ی اینا رو می‌تونم همین‌جا، با کد واقعی و قابل‌آپلود بنویسم:

- **کل سیستم پایه**: سیستم مقیاس سیال، متغیرهای رنگ، تایپوگرافی، گرید
- **سیستم انیمیشن کامل**: هر ۷ کامپوننت ریویل بالا
- **۸ ماژول از ۹ ماژول** صفحه اول
- هدر/فوتر/منو/لودر/مودال
- کاروسل (بدون کتابخونه، وانیلا)
- گرید محصولات + فیلتر + سورت
- ریسپانسیو کامل موبایل
- بهینه‌سازی سرعت (lazy load, preload)

### 🟡 نیاز به دارایی از تو (نه ابزار دیگه)

- **ویدیوی هیرو**: من کد اسکراب رو می‌نویسم، ولی خود ویدیو رو باید تولید کنی (همون کاری که با نانو بنانا داریم می‌کنیم). یه ویدیوی ۵-۱۰ ثانیه‌ای از نزدیک محصول کافیه.
- **عکس‌های محصول در فضا**: همون فرآیند FRAME که داریم جلو می‌بریم.
- **متن‌های واقعی**: عنوان‌ها، توضیحات دسته‌بندی‌ها.

### 🔴 تصمیم‌های تو (نه فنی)

- **CMS**: Kettal از DatoCMS استفاده می‌کنه. تو WooCommerce داری. باید تصمیم بگیری سایت جدید روی وردپرس بمونه یا نه — این تصمیم معماری کل پروژه‌ست. (پیشنهاد من پایین)
- **فاضل چقدر درگیر باشه**: من می‌تونم کد آماده بدم، ولی دیپلوی و اتصال به دیتابیس محصولات کار اونه.

**درباره‌ی «قسمت design»**: صادقانه — برای این پروژه لازمش نداریم. اون برای وقتیه که بخوایم یه دیزاین *جدید* از صفر خلق کنیم. ولی ما دیزاین رو داریم (Kettal)، مسئله اجرای فنی دقیقشه، که کار منه. اگه بعداً خواستیم صفحاتی بسازیم که Kettal نداره (مثلاً صفحه‌ی «درباره‌ی ما»ی اختصاصی اپن‌تاپ)، اون‌موقع ازش استفاده می‌کنیم.

---

## ⚠️ تصمیم بزرگی که باید بگیری: وردپرس یا نه

این مهم‌ترین سؤال کل پروژه‌ست و جواب من ممکنه اذیتت کنه:

**Kettal یه سایت وردپرسی نیست و این تصادفی نیست.** این سطح از انیمیشن روی وردپرس یا خیلی سخته یا کند می‌شه.

سه گزینه:

**الف) استاتیک خالص (HTML/CSS/JS)** — سریع‌ترین راه رسیدن به نتیجه. محصولات هاردکد. مناسب اگه کاتالوگ ثابته و به‌روزرسانی کم داره. من می‌تونم کاملش رو تحویل بدم. ❗ولی مدیریت محصول از پنل نداری.

**ب) وردپرس + قالب اختصاصی** — همون WooCommerce فعلی بمونه، من قالب رو بنویسم. ✅ پنل مدیریت داری. ⚠️ بعضی انیمیشن‌ها سنگین‌تر درمیان و فاضل باید PHP بلد باشه.

**ج) Nuxt مثل خودشون** — دقیقاً همون کیفیت. ⚠️ ولی فاضل باید Vue بلد باشه، و کل زیرساخت عوض می‌شه.

**پیشنهاد من: گزینه (ب)**، ولی با یه ترفند — قالب رو طوری بنویسم که HTML/CSS/JSـش کاملاً مستقل باشه، فقط داده از وردپرس بگیره. اینطوری اگه بعداً خواستی مهاجرت کنی، ظاهر سایت دست‌نخورده منتقل می‌شه.

---

## مسیر سریع ساخت (پیشنهادی)

**فاز ۰ — تصمیم** (تو): وردپرس یا استاتیک؟ ← بدون این نمی‌شه شروع کرد

**فاز ۱ — هسته** (من، سریع): سیستم مقیاس سیال + متغیرها + تایپوگرافی + هر ۷ کامپوننت ریویل. این «موتور» سایته؛ وقتی این باشه، بقیه‌ی ماژول‌ها سریع سوار می‌شن.

**فاز ۲ — یک ماژول نمونه** (من): یکی از ماژول‌های سخت‌تر (مثلاً `card-group-stairs`) رو کامل می‌سازم تا **تو و فاضل ببینید کیفیت درمیاد یا نه** — قبل از اینکه وقت زیادی صرف بقیه بشه.

**فاز ۳ — بقیه‌ی ماژول‌ها** (من): ۸ ماژول باقی‌مانده.

**فاز ۴ — هیرو ویدیو** (مشترک): تو ویدیو رو تولید کن، من کد اسکراب رو بنویسم.

**فاز ۵ — اتصال داده** (فاضل): وصل‌کردن به محصولات واقعی.

---

## قدم بعدی

دو تا چیز ازت می‌خوام:
1. **جواب سؤال وردپرس/استاتیک** (فاز ۰)
2. اگه می‌خوای مطمئن بشی روش جواب می‌ده، بگو **فاز ۲ رو اول بسازم** — یه ماژول کامل و واقعی، که ببینی کیفیتش به Kettal می‌رسه یا نه، قبل از سرمایه‌گذاری روی کل پروژه.
